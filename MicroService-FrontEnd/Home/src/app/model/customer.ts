interface CustomerReader
{
    id: string;
    name: string;
    address: string;
    phoneNumber: string;
    email: string;    
}

interface CustomerWriter
{
    name: string;
    address: string;
    phoneNumber: string;
    email: string;    
}